# 复制本文件为 .env 并填入真实密钥（.env 已被 gitignore，不会提交）

# DeepSeek LLM API Key（RAG 问答，config.ini [llm] 通过 ${DEEPSEEK_API_KEY} 引用）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# 阿里云百炼业务空间 API Key（千问 VL 图片识别，config.ini [vlm] 通过 ${DASHSCOPE_API_KEY} 引用）
DASHSCOPE_API_KEY=
